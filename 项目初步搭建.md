# 豪华汽车租赁后台管理系统 - Java 后端服务开发提示词

> Markdown 文档，可直接复制保存为 `.md`，投喂 AI 生成后端完整项目，接口完全对齐你的 React 豪车租赁前端

```
# 豪华汽车租赁后台管理系统 Java后端开发提示词
## 一、前置基础约束（强制要求）
1. 项目背景
前端为纯React18豪华汽车租赁后台管理系统（非React Native），前端已完成全部页面交互、GSAP登录动画、全局主题配置、RBAC权限体系、全套租车业务模块。本次开发**Java后端服务，所有接口路径、请求参数、返回字段严格对齐前端现有Axios请求代码，实现无缝联调**。

2. 技术栈强制规范
- 核心框架：SpringBoot 2.7.x / SpringBoot3（优先SpringBoot3 + JDK17）
- ORM框架：MyBatis-Plus
- 数据库：MySQL 8.0
- 权限认证：JWT + SpringSecurity
- 缓存：Redis（存储token、权限信息、仪表盘统计缓存）
- 工具集：Hutool
- 文件上传：本地存储方案（可预留对接OSS拓展入口）
- 构建工具：Maven
- API风格：RESTful 规范

3. 前后端对接规范
- 请求分页参数统一使用 `pageNum`、`pageSize`，和前端保持一致
- 鉴权方式：请求头携带 `token`，与前端登录逻辑完全匹配
- 开启全局跨域配置，适配前端本地开发代理
- 统一全局返回结果结构，匹配前端Axios响应拦截器，禁止自定义格式
```json
{
  "code": 200,
  "msg": "提示信息",
  "data": {}
}
```

状态码约定：

200 = 成功；401=Token 失效 / 未登录；403 = 无访问权限；500 = 服务异常

- 时间字段统一使用 DateTime，前后端日期格式统一

1. 交付目标

   后端项目完整可运行，附带数据库初始化 SQL；启动后前端无需大规模修改 API 代码，直接开展联调。

   所有开发内容必须兼容前端全部功能，不删减任何业务接口。

## 二、前端全部业务模块（后端必须完整实现接口）

### 1. 认证模块

1. 管理员账号登录、退出登录

2. 账号密码校验、JWT 签发、设置 Token 有效期

3. Token 有效性校验、登录鉴权

4. 用户重置密码接口

   

   业务约束：

- 禁用账号禁止登录；超级管理员拥有特殊权限，不可被删除、禁用

### 2. 系统设置模块（完整 RBAC 权限体系）

#### 2.1 用户管理

- 用户分页列表、多条件搜索筛选
- 新增用户、编辑用户、删除用户、批量操作
- 账号启用 / 禁用、重置密码
- 用户支持绑定多个角色
- 记录用户最后登录时间

#### 2.2 角色管理

- 角色分页列表、新增、编辑、删除、启用 / 禁用

- 角色分配菜单权限（返回树形权限结构）

  

  业务约束：

- 已绑定用户的角色禁止删除

- 超级管理员内置角色禁止删除、禁用

#### 2.3 菜单管理

- 树形菜单列表，支持展开收起

- 新增一级菜单 / 子菜单、编辑菜单、删除菜单

- 菜单排序、启用 / 禁用、侧边栏隐藏控制

- 对外提供权限树接口，供角色管理调用

  

  业务约束：

- 存在子菜单的父菜单禁止删除

- 系统基础核心菜单禁止删除

#### 2.4 全局主题配置

- 保存用户个性化主题配置（布局、主题色、各类开关参数）
- 获取当前用户主题配置
- 支持配置导入导出接口
- 配置持久化存入数据库

### 3. 仪表盘首页模块

- 统计接口：总订单数量、在租车辆、闲置车辆、今日营收、本月营收
- 图表数据：订单趋势折线、车辆分类占比、营收统计柱状图
- 最新订单列表、最新租客动态列表

### 4. 车辆管理模块

- 车辆分页列表、多条件筛选搜索
- 新增车辆、编辑车辆、上架、下架、删除车辆
- 车辆多图片上传接口
- 车辆状态管控：闲置 / 租赁中 / 维修
- 车辆信息完整字段存储（车型、车牌、日租价、押金、座位、排量、车辆介绍等）

### 5. 租客（客户）管理模块

- 租客分页列表、新增、编辑、删除
- 租客账号启用 / 禁用
- 租客租赁历史记录查询
- 存储租客手机号、实名认证相关信息

### 6. 订单管理模块

- 订单分页列表，支持按状态筛选：待付款、租赁中、已完成、已取消、逾期订单
- 查询订单详情、手动修改订单状态
- 租期、租金、押金、客户车辆关联信息查询
- 订单导出接口

### 7. 财务统计模块

- 租金流水记录、押金流水记录
- 按日 / 月 / 年营收统计接口

## 三、通用基础能力需求

1. 文件上传接口：支持车辆图片单图、多图上传，返回完整访问 URL
2. 全局分页统一封装，复用分页 DTO
3. 全局异常统一捕获处理
4. SpringSecurity 权限拦截器：基于权限标识实现接口级权限控制
5. 操作日志功能（可选拓展）：记录新增、编辑、删除操作人、操作时间、操作内容

## 四、数据库设计要求

1. 输出完整可执行 MySQL8.0 建表 SQL 脚本，包含以下数据表：

- sys_user（后台管理员用户表）
- sys_role（角色表）
- sys_user_role（用户角色关联中间表）
- sys_menu（菜单权限表）
- sys_theme_config（用户主题配置表）
- car_info（车辆信息表）
- customer_info（租客客户表）
- customer_order（客户订单表，对齐官网下单页面）
- finance_record（财务流水表）

1. 数据表规范：

- 所有业务表使用**逻辑删除（is_delete）**，不物理删除数据
- 合理设置索引、字段长度、非空约束
- 密码加密存储，禁止明文保存

1. 初始化基础数据 SQL：

   

   内置超级管理员账号、默认角色、系统全套菜单基础数据，启动项目导入即可使用

## 五、安全规范强制要求

1. 用户密码使用 BCrypt 加密存储
2. JWT 支持主动失效（退出登录时 Redis 加入黑名单）
3. 接口入参校验，防止非法参数请求
4. 权限校验：普通账号无法操作超级管理员相关数据，防止越权访问
5. 敏感接口增加基础防护

## 六、项目代码架构规范

plaintext







```
src/main/java/com/car/rental
├── CarRentalApplication.java        # 启动类
├── config                           # 配置类
│   ├── SecurityConfig.java          # SpringSecurity配置
│   ├── RedisConfig.java
│   ├── MybatisPlusConfig.java
│   └── GlobalCorsConfig.java
├── common                           # 公共通用模块
│   ├── result                       # 统一返回结果封装
│   ├── dto                          # 通用DTO、分页DTO
│   ├── exception                    # 全局异常处理器
│   ├── util                         # 工具类(Jwt、加密等)
│   └── annotation                   # 自定义权限注解
├── module                           # 业务模块分层
│   ├── auth                         # 登录认证模块
│   ├── system                       # 系统管理：用户、角色、菜单、主题配置
│   ├── car                          # 车辆管理模块
│   ├── customer                     # 租客管理模块
│   ├── order                        # 订单管理模块
│   ├── statistics                   # 仪表盘、财务统计模块
│   └── upload                       # 文件上传模块
├── entity                           # Mybatis-Plus实体类
├── mapper                           # Mapper接口
├── service                          # Service接口 + 实现类
└── controller                       # 控制器
```

分层规范：Controller → Service → Mapper → Entity

- Controller：接收请求、参数校验
- Service：封装所有业务逻辑
- Mapper：数据库交互
- DTO：入参接收、返回 VO 定义

## 七、AI 交付产出硬性要求

1. 输出完整 Java 后端全部代码，目录结构完整，Maven 依赖齐全
2. 完整 MySQL 建表 SQL + 初始化基础数据脚本
3. 项目部署启动文档：环境要求、数据库配置修改位置、启动命令
4. 接口清单文档：接口地址、请求方式、入参、返回示例
5. 前后端联调说明：跨域配置、Token 传递规则、字段对齐注意事项
6. 代码增加清晰注释，预留拓展接口，方便后续迭代
7. 优先对齐前端现有 Mock 接口逻辑，接口行为保持一致；遇到字段名称冲突主动标注，禁止随意自定义字段

## 八、重要备注

生成接口时，**优先参考前端 api 请求地址、请求参数名称**，尽可能保持一致；如果前端存在 Mock 模拟接口，后端真实接口逻辑需要和 Mock 行为保持统一，最大限度降低前端改动成本。